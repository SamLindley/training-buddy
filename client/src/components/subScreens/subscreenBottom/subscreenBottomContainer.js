import React from 'react';
import PropTypes from 'prop-types';

const SubescreenBottomContainer = props => {
  return (
    <div style={styles}>
      BOTTOM
    </div>
  );
};

const styles = {
  height: "50%",
  width: "100%",
  background: "#333"
};

SubescreenBottomContainer.propTypes = {};

export default SubescreenBottomContainer;
