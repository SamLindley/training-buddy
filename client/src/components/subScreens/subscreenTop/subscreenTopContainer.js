import React from 'react';
import PropTypes from 'prop-types';

const SubscreenTopContainer = props => {
  return (
    <div style={style}>
      TOP
    </div>
  );
};

const style = {
  height: "50%",
  background: "#666"
}

SubscreenTopContainer.propTypes = {

};

export default SubscreenTopContainer;
